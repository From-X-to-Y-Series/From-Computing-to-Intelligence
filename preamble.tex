\usepackage[
    paperwidth=203mm,
    paperheight=254mm,
    margin=0.875in,  % Overall margin
    inner=0.9in,
    outer=0.5in
]{geometry}


% Font settings for main text and math
\usepackage{mathptmx}       % Use Time-like font for text and math
\usepackage[utf8]{inputenc}  % UTF-8 encoding
\usepackage[T1]{fontenc}     % T1 font encoding for better hyphenation and special characters


% Additional packages for typesetting and layout
\usepackage{amsmath, amssymb, mathtools} % Essential math packages
\usepackage{microtype}        % Improves text spacing and justification
\usepackage{graphicx}         % For including images
\usepackage{hyperref}         % For clickable links
\usepackage{enumitem}         % For custom enum list labels 

% Disable hyphenation and adjust paragraph spacing
\usepackage[none]{hyphenat}   % Disable hyphenation throughout the document
\setlength{\parindent}{0pt}   % Disable paragraph indentation
\setlength{\parskip}{0.6em}   % Increase spacing between paragraphs

\setlength{\headheight}{14pt}

% Placeholder text package for layout testing
\usepackage{lipsum}

% To add blank page whenever required
\usepackage{afterpage}
\newcommand\blankpage{%
    \null
    \thispagestyle{empty}%
    \newpage}

% Reduce spacing before and after subsections
\usepackage{titlesec}
\titlespacing*{\section}{0pt}{1.5ex plus .2ex minus .2ex}{1ex plus .2ex}
\titlespacing*{\subsection}{0pt}{1.2ex plus .2ex minus .2ex}{1ex plus .2ex}

% Set up hyperref color scheme for a more professional look
\hypersetup{
    citecolor=blue,
    urlcolor=blue
}

% Use custom styling for page setup
\usepackage{styles/page}

% Adding tikz package for diagrams
\usepackage{tikz}
\usetikzlibrary{patterns}  % For using colors and patterns like fill lines in a circle
\usetikzlibrary{arrows.meta}

% To update the spacing between table of contents entries
\usepackage{tocloft}
\setlength{\cftbeforesecskip}{3pt} % Adjust spacing before section entries
\setlength{\cftbeforesubsecskip}{1pt} % Adjust spacing before subsection entries

% Equation numbering
\numberwithin{equation}{chapter}

% Theorem Environments
\usepackage{amsthm}

\usepackage{tabularx}

\newtheoremstyle{plainsolution}% name
    {\topsep}%                  Space above
    {\topsep}%                  Space below
    {\normalfont}%              Body font (normal font instead of italic)
    {}%                         Indent amount
    {\bfseries}%               Theorem head font
    {.}%                       Punctuation after theorem head
    {.5em}%                    Space after theorem head
    {}%                        Theorem head spec

\theoremstyle{plainsolution}
\newtheorem{solution}{Solution}[chapter]

% Other theorem environments can use the default style
\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[chapter]
\newtheorem{definition}{Definition}[chapter]
\newtheorem{lemma}{Lemma}[chapter]
\newtheorem{corollary}{Corollary}[chapter]
\newtheorem{example}{Example}[chapter]
\newtheorem{exercise}{Exercise}[chapter]

\usepackage{booktabs} % For \toprule command in tables
\usepackage{subfig}

\usepackage{tcolorbox}
\tcbuselibrary{listings, skins}

% Custom algorithm box environment
\newtcolorbox{algobox}[2][]{%
  enhanced,
  colback=white,
  colframe=black,
  fonttitle=\bfseries,
  title={#2},
  #1,
  boxsep=1mm,
  left=2mm,
  right=2mm,
  top=1mm,
  bottom=1mm,
  sharp corners,
  before skip=10pt,
  after skip=10pt
}

\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{calc,positioning}


\usepackage{xcolor}
\usepackage{listings}
\usepackage{tcolorbox}

% Define a very light gray background color
\definecolor{verylightgray}{rgb}{0.95,0.95,0.95}

% Define the codebox environment with smaller, monospace font suitable for Python code (no syntax highlighting)
\tcbset{
  codebox/.style={
    colback=verylightgray,
    colframe=gray!30,
    boxrule=0.5pt,
    arc=4pt,
    auto outer arc,
    boxsep=5pt,
    left=5pt,
    right=5pt,
    top=5pt,
    bottom=5pt,
    listing only,
    listing options={
      basicstyle=\ttfamily\footnotesize, % Use \ttfamily for monospace font
      breaklines=true,
      columns=fullflexible,
      keepspaces=true,
      showspaces=false,
      showstringspaces=false,
      numbers=none,
      frame=none,
      language=Python  % sets font conventions but no keyword colors by default here
    },
  }
}


% Custom Titlepage
\newcommand{\maketitlepage}{
    \begin{titlepage}
        \centering
        \vspace*{2cm}
        {\Huge\bfseries From Computing to Intelligence\par}
        \vspace{0.15cm}
        {\Large \textbf{A First-Principles Approach to Artificial Intelligence \\in Language, Vision, and Audio}\par}
        \vspace*{5cm}
        {\Large \textbf{Sagar Udasi}\par}
        \vspace*{0.2cm}
        \textit{MSc Statistics with Data Science and Computational Finance}\\
        \vspace*{0.1cm}
        \textit{University of Edinburgh}
        \vfill
    \end{titlepage}
}